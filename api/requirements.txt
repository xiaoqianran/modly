# Web server
fastapi>=0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Agent & MCP
httpx>=0.27.0
mcp>=1.0.0

# Mesh processing (optimize + export endpoints)
trimesh>=4.5.0
pymeshlab>=2023.12
# Official TRELLIS generator.py does `from PIL import Image` at import.
# Without this the Modal image drops the extension and Generate 400s Unknown model.
pillow>=10.0.0

# Model downloads (HuggingFace)
huggingface_hub>=0.27.0
hf_xet>=0.1.0

cryptography>=42.0.0
certifi>=2024.0.0
